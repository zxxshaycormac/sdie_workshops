# OpenSpec Preparation


`pnpm add -g @fission-ai/openspec@latest`

`openspec init`


## 获取代码的基础结构

```
use `cloc` cli to give this project a summary, base on the structure of the project, not only simple run 'cloc' by root folder of the project.

write into @workshops/basic/code_summary.md
```

## 创建设计基线

/superpowers:brainstorming 通过现有的代码去探知一下所有的设计规范和设计标准，记录在设计的文档里 design.md
通过 @workshops/basic/code_summary.md 了解代码基本结构
